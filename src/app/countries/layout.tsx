import React from 'react'

export const metadata = {
    title: 'countries | Journey Rover'
}

export default function layout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}
